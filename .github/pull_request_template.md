## Summary

<!-- What does this PR change and why? -->

## SDK(s) touched

- [ ] Node (`node/`)
- [ ] Python (`python/`)
- [ ] Go (`go/`)
- [ ] CI / docs / repo-wide

## Checklist

- [ ] Tests added or updated for every changed code path
- [ ] Wire-format changes applied to **all three** SDKs (or explained below why not)
- [ ] Package README updated when behavior changed
- [ ] `CHANGELOG.md` updated
- [ ] Local checks pass (`npm run lint && npm test` / `ruff check . && pytest` / `go vet ./... && go test ./...`)
